# Phase 2 Pseudocode: block_00600000 (0x00600040 - 0x00607920)

## Summary

| Metric | Count |
|--------|-------|
| Total functions | 103 |
| FW.crt | 103 |
| RN.sprite | 0 |

This block is **100% MSVC 6.0 CRT** (Visual Studio 1998 Debug). Zero game logic. All functions were auto-identified by Ghidra FID. No game-state globals (DAT_006xxxxx) are referenced — only CRT-internal state in 0x006e/0x0063 ranges.

### Porting Status

| Status | Count | Notes |
|--------|-------|-------|
| SK | 103 | All CRT — skip entirely |

---

## Functions

### 1. __NMSG_WRITE @ 0x00600040

**Size**: 537 bytes | **Category**: FW.crt | **Porting**: SK

Writes CRT runtime error message to stderr (console mode) or MessageBox (GUI mode). Iterates error table at DAT_0063b1b8 to find message by error number. Calls `__CrtDbgReport` for debug breaks.

---

### 2. __GET_RTERRMSG @ 0x00600260

**Size**: small | **Category**: FW.crt | **Porting**: SK

Returns pointer to runtime error message string for a given message number. Linear scan of the error table.

---

### 3. ___loctotime_t @ 0x006002E0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Converts local time components (yr, mo, dy, hr, mn, sc, dst) to `time_t`. Validates ranges, applies DST offset, accounts for timezone bias.

---

### 4. _abort @ 0x00600400

**Size**: stub | **Category**: FW.crt | **Porting**: SK

CRT `abort()`. Raises SIGABRT, then calls `_exit(3)`.

---

### 5. _signal @ 0x00600430

**Size**: medium | **Category**: FW.crt | **Porting**: SK

CRT `signal()`. Installs a signal handler for the given signal number. Validates signum, stores handler in signal table. Registers console control event handler via `SetConsoleCtrlHandler` for SIGINT.

---

### 6. ctrlevent_capture @ 0x00600610

**Size**: small | **Category**: FW.crt | **Porting**: SK

Windows console control event handler (Ctrl+C, Ctrl+Break). Maps console events to SIGINT/SIGBREAK and calls `raise()`.

---

### 7. _raise @ 0x006006A0

**Size**: medium | **Category**: FW.crt | **Porting**: SK

CRT `raise()`. Dispatches to the installed signal handler for the given signal number. Handles SIGFPE sub-codes. Resets handler to SIG_DFL after invocation (SysV semantics).

---

### 8. siglookup @ 0x006008C0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Looks up signal handler entry in the CRT signal table by signal number. Returns pointer to handler slot.

---

### 9. ___crtMessageBoxA @ 0x00600930

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT wrapper for `MessageBoxA`. Lazy-loads `user32.dll` on first call (stores proc addresses in DAT_0063b260..0063b268). Falls back to no-op if DLL unavailable.

---

### 10. __itoa @ 0x00600A10

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_itoa()`. Integer to ASCII string conversion. Delegates to `xtoa()` with signed flag.

---

### 11. xtoa @ 0x00600A70

**Size**: small | **Category**: FW.crt | **Porting**: SK

Core unsigned-to-ASCII helper. Divides value by radix, builds digit string in reverse, then reverses in-place. Handles negative prefix for signed mode.

---

### 12. __ltoa @ 0x00600B30

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_ltoa()`. Long to ASCII string. Wrapper around `xtoa()`.

---

### 13. __ultoa @ 0x00600B90

**Size**: stub | **Category**: FW.crt | **Porting**: SK

CRT `_ultoa()`. Unsigned long to ASCII string. Wrapper around `xtoa()` with unsigned flag.

---

### 14. __i64toa @ 0x00600BC0

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_i64toa()`. 64-bit integer to ASCII. Delegates to `x64toa()` with signed flag.

---

### 15. x64toa @ 0x00600C30

**Size**: small | **Category**: FW.crt | **Porting**: SK

Core 64-bit unsigned-to-ASCII helper. Same algorithm as `xtoa()` but operates on (lo, hi) pair with 64-bit division.

---

### 16. __ui64toa @ 0x00600D10

**Size**: stub | **Category**: FW.crt | **Porting**: SK

CRT `_ui64toa()`. Unsigned 64-bit integer to ASCII. Wrapper around `x64toa()`.

---

### 17. _fprintf @ 0x00600D40

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `fprintf()`. Formats output to a FILE stream. Sets up va_list, calls internal `_output()` formatter.

---

### 18. _setvbuf @ 0x00600DF0

**Size**: medium | **Category**: FW.crt | **Porting**: SK

CRT `setvbuf()`. Sets stream buffering mode (_IONBF, _IOLBF, _IOFBF). Frees old buffer if heap-allocated, assigns new buffer or allocates one.

---

### 19. $E2 @ 0x00600F50

**Size**: stub | **Category**: FW.crt | **Porting**: SK

CRT `atexit` handler. Calls `$E1`.

---

### 20. $E1 @ 0x00600F70

**Size**: stub | **Category**: FW.crt | **Porting**: SK

CRT `atexit` handler. Installs `_CxxUnhandledExceptionFilter` via `SetUnhandledExceptionFilter`.

---

### 21. __CxxUnhandledExceptionFilter @ 0x00600FA0

**Size**: small | **Category**: FW.crt | **Porting**: SK

C++ unhandled exception filter. Checks if the exception is a C++ exception (code 0xE06D7363); if so, calls `terminate()`. Otherwise returns EXCEPTION_CONTINUE_SEARCH.

---

### 22. FUN_00601040 — `crt_restore_exception_filter` @ 0x00601040

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Restores the original unhandled exception filter saved in DAT_006e55ac via `SetUnhandledExceptionFilter`.

---

### 23. _ValidateRead @ 0x00601060

**Size**: small | **Category**: FW.crt | **Porting**: SK

Debug CRT read-pointer validation. Calls `IsBadReadPtr(ptr, size)`. Returns 1 if valid.

---

### 24. _ValidateWrite @ 0x006010A0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Debug CRT write-pointer validation. Calls `IsBadWritePtr(ptr, size)`. Returns 1 if valid.

---

### 25. _ValidateExecute @ 0x006010E0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Debug CRT function-pointer validation. Calls `IsBadCodePtr(ptr)`. Returns 1 if valid.

---

### 26. __snprintf @ 0x00601120

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_snprintf()`. Formats to buffer with size limit. Sets up va_list, calls `_vsnprintf()`.

---

### 27. __vsnprintf @ 0x00601210

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_vsnprintf()`. Formats to buffer with size limit using va_list. Creates a temporary FILE-like struct with buffer/count, calls `_output()`.

---

### 28. ___crtGetStringTypeW @ 0x00601300

**Size**: medium | **Category**: FW.crt | **Porting**: SK

CRT wrapper for `GetStringTypeW`. Falls back to multibyte conversion + `GetStringTypeA` if wide version unavailable. Caches API availability in DAT_0063b26c.

---

### 29. ___crtGetStringTypeA @ 0x00601560

**Size**: medium | **Category**: FW.crt | **Porting**: SK

CRT wrapper for `GetStringTypeA`. Falls back to wide conversion + `GetStringTypeW` if needed. Caches API availability in DAT_0063b270.

---

### 30. __open @ 0x00601700

**Size**: stub | **Category**: FW.crt | **Porting**: SK

CRT `_open()`. Forwards to `_sopen()` with default sharing flags (no sharing).

---

### 31. __sopen @ 0x00601750

**Size**: xlarge | **Category**: FW.crt | **Porting**: SK

CRT `_sopen()`. Opens a file with sharing mode. Maps CRT flags to Win32 `CreateFileA` parameters. Handles O_CREAT, O_TRUNC, O_EXCL, O_APPEND, O_TEXT/O_BINARY. Allocates CRT file descriptor via `_alloc_osfhnd`.

---

### 32. __alloc_osfhnd @ 0x00601CF0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Allocates a CRT file descriptor slot for an OS handle. Scans the ioinfo table for a free slot.

---

### 33. __set_osfhnd @ 0x00601E40

**Size**: small | **Category**: FW.crt | **Porting**: SK

Associates an OS file handle with a CRT file descriptor. Stores handle in the ioinfo table entry.

---

### 34. __free_osfhnd @ 0x00601F40

**Size**: small | **Category**: FW.crt | **Porting**: SK

Releases an OS handle from a CRT file descriptor. Sets the ioinfo entry's handle to -1.

---

### 35. __get_osfhandle @ 0x00602060

**Size**: small | **Category**: FW.crt | **Porting**: SK

Returns the OS handle (HANDLE) for a CRT file descriptor. Validates descriptor range and state.

---

### 36. __open_osfhandle @ 0x006020E0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Creates a CRT file descriptor from an existing OS handle. Maps access flags, allocates fd via `_alloc_osfhnd`.

---

### 37. __commit @ 0x006021E0

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_commit()`. Flushes file buffers to disk via `FlushFileBuffers`. Validates fd first.

---

### 38. __isatty @ 0x006022C0

**Size**: stub | **Category**: FW.crt | **Porting**: SK

CRT `_isatty()`. Returns nonzero if the file descriptor refers to a console/character device. Checks the FDEV flag in ioinfo.

---

### 39. _wctomb @ 0x00602310

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `wctomb()`. Converts a wide character to multibyte. Uses `WideCharToMultiByte` with the current locale's code page.

---

### 40. __aulldiv @ 0x006023E0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Compiler helper: unsigned 64-bit division. Takes two 64-bit values as (lo, hi) pairs, returns quotient.

---

### 41. __aullrem @ 0x00602450

**Size**: small | **Category**: FW.crt | **Porting**: SK

Compiler helper: unsigned 64-bit remainder. Takes two 64-bit values as (lo, hi) pairs, returns remainder.

---

### 42. __fcloseall @ 0x006024D0

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_fcloseall()`. Closes all open FILE streams except stdin/stdout/stderr. Iterates stream table, calls `fclose` on each.

---

### 43. __statusfp @ 0x00602590

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Gets FPU status word via `fstsw` instruction. Returns abstract status flags.

---

### 44. __clearfp @ 0x006025C0

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Clears FPU exception flags via `fnclex`. Returns the old status word before clearing.

---

### 45. __control87 @ 0x006025F0

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Gets/sets FPU control word. Reads current word via `fstcw`, applies mask and new value, writes back via `fldcw`.

---

### 46. __controlfp @ 0x00602640

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Portable version of `_control87`. Masks off reserved bits before forwarding to `_control87`.

---

### 47. __fpreset @ 0x00602670

**Size**: small | **Category**: FW.crt | **Porting**: SK

Resets FPU to default state via `fninit`. Restores the default control word (round-nearest, 53-bit precision, all exceptions masked).

---

### 48. __abstract_cw @ 0x006026D0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Converts hardware x87 FPU control word to CRT abstract form. Maps precision, rounding, and exception-mask bits to portable constants.

---

### 49. __hw_cw @ 0x00602820

**Size**: small | **Category**: FW.crt | **Porting**: SK

Converts CRT abstract FPU control word back to hardware x87 form. Inverse of `_abstract_cw`.

---

### 50. __abstract_sw @ 0x006029F0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Converts hardware x87 FPU status word to CRT abstract form. Maps exception-occurred bits to portable constants.

---

### 51. __fptrap @ 0x00602A70

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Raises SIGFPE signal (floating-point exception trap).

---

### 52. __ZeroTail @ 0x00602A90

**Size**: small | **Category**: FW.crt | **Porting**: SK

Checks if all mantissa digits after position `pos` are zero. Used by long-double rounding logic. Returns 1 if tail is zero.

---

### 53. __IncMan @ 0x00602B30

**Size**: small | **Category**: FW.crt | **Porting**: SK

Increments a multi-word mantissa at the given digit position. Propagates carry through higher words. Returns nonzero on overflow.

---

### 54. __RoundMan @ 0x00602BF0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Rounds a multi-word mantissa at position `pos`. Checks digit at pos: if >5 or (==5 and tail nonzero) → increment. Returns rounding direction.

---

### 55. __CopyMan @ 0x00602CD0

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Copies a multi-word mantissa (4 dwords) from src to dst.

---

### 56. __FillZeroMan @ 0x00602D20

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Zeros out a multi-word mantissa (4 dwords).

---

### 57. __IsZeroMan @ 0x00602D60

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Tests if a multi-word mantissa is all zeros. Returns 1 if zero.

---

### 58. __ShrMan @ 0x00602DB0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Right-shifts a multi-word mantissa by `count` bits. Operates on 4 dwords with carry propagation.

---

### 59. __ld12cvt @ 0x00602EA0

**Size**: large | **Category**: FW.crt | **Porting**: SK

Converts a 12-byte (96-bit) long double to a target floating-point format. Handles denormals, overflow, underflow, and rounding. Core conversion engine used by `__ld12tod` and `__ld12told`.

---

### 60. FID_conflict:__ld12tod (A) @ 0x00603110

**Size**: stub | **Category**: FW.crt | **Porting**: SK

12-byte long double to `double` conversion (variant A). Calls `__ld12cvt` with IEEE 754 double parameters.

---

### 61. FID_conflict:__ld12tod (B) @ 0x00603140

**Size**: stub | **Category**: FW.crt | **Porting**: SK

12-byte long double to `double` conversion (variant B). Duplicate entry, same logic as variant A.

---

### 62. __ld12told @ 0x00603170

**Size**: small | **Category**: FW.crt | **Porting**: SK

12-byte long double to 10-byte `long double` (80-bit extended) conversion. Truncates the 12-byte internal format.

---

### 63. FID_conflict:__atodbl (A) @ 0x00603240

**Size**: stub | **Category**: FW.crt | **Porting**: SK

ASCII string to `double` (variant A). Parses string via `__strgtold12`, converts via `__ld12tod`.

---

### 64. __atoldbl @ 0x00603280

**Size**: stub | **Category**: FW.crt | **Porting**: SK

ASCII string to `long double`. Parses string via `__strgtold12`, converts via `__ld12told`.

---

### 65. FID_conflict:__atodbl (B) @ 0x006032C0

**Size**: stub | **Category**: FW.crt | **Porting**: SK

ASCII string to `double` (variant B). Duplicate entry, same logic as variant A.

---

### 66. __fptostr @ 0x00603300

**Size**: small | **Category**: FW.crt | **Porting**: SK

Converts floating-point value to decimal string. Rounds mantissa to requested digits, handles sign and zero cases.

---

### 67. __fltout @ 0x006033E0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Float output helper. Converts double to 12-byte long double via `___dtold`, then calls `_I10_OUTPUT` for decimal output.

---

### 68. ___dtold @ 0x00603450

**Size**: small | **Category**: FW.crt | **Porting**: SK

Converts `double` (64-bit) to 12-byte long double internal format. Expands exponent and mantissa fields.

---

### 69. _wcslen @ 0x006035D0

**Size**: stub | **Category**: FW.crt | **Porting**: SK

CRT `wcslen()`. Returns length of wide-character string (counts `wchar_t` units until null terminator).

---

### 70. ___tzset @ 0x00603620

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Internal timezone initialization. One-time guard that calls `_tzset()` on first invocation.

---

### 71. __tzset @ 0x00603650

**Size**: large | **Category**: FW.crt | **Porting**: SK

CRT `_tzset()`. Reads the `TZ` environment variable, parses timezone name/offset/DST rules, sets globals `_timezone`, `_daylight`, `_tzname`, `_dstbias`.

---

### 72. __isindst @ 0x006039B0

**Size**: large | **Category**: FW.crt | **Porting**: SK

Checks if a given `tm` time falls within the DST period. Uses timezone transition rules parsed by `_tzset()` and converted by `cvtdate()`.

---

### 73. cvtdate @ 0x00603D20

**Size**: medium | **Category**: FW.crt | **Porting**: SK

Converts timezone transition date specification (month/week/day-of-week or month/day) to day-of-year. Handles "first Sunday in April" style rules.

---

### 74. __chsize @ 0x00603F30

**Size**: medium | **Category**: FW.crt | **Porting**: SK

CRT `_chsize()`. Truncates or extends a file to the given size. Uses `SetFilePointer` + `SetEndOfFile`. Pads with zeros when extending.

---

### 75. ___addl @ 0x006041E0

**Size**: stub | **Category**: FW.crt | **Porting**: SK

32-bit addition with carry output. Returns carry flag. Used by 12-byte long double arithmetic.

---

### 76. ___add_12 @ 0x00604230

**Size**: small | **Category**: FW.crt | **Porting**: SK

12-byte (3-dword) addition. Adds src to dst with carry propagation across the 3 dwords.

---

### 77. ___shl_12 @ 0x006042E0

**Size**: small | **Category**: FW.crt | **Porting**: SK

12-byte left shift by 1 bit. Shifts all 3 dwords with carry propagation from low to high.

---

### 78. ___shr_12 @ 0x00604360

**Size**: small | **Category**: FW.crt | **Porting**: SK

12-byte right shift by 1 bit. Shifts all 3 dwords with carry propagation from high to low.

---

### 79. ___mtold12 @ 0x006043E0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Converts a decimal mantissa string (digit characters) and exponent into a 12-byte long double. Builds the value digit-by-digit using multiply-by-10 and add.

---

### 80. ___strgtold12 @ 0x00604520

**Size**: xlarge (2795 bytes) | **Category**: FW.crt | **Porting**: SK

Master string-to-long-double parser. Handles sign, integer part, decimal point, fractional part, exponent (E notation), INF, NAN. Produces a 12-byte long double. The largest function in this block.

---

### 81. ___STRINGTOLD @ 0x00605140

**Size**: small | **Category**: FW.crt | **Porting**: SK

Public wrapper for `___strgtold12`. Converts result from 12-byte format to 10-byte `long double`.

---

### 82. $I10_OUTPUT @ 0x006051A0

**Size**: large (1335 bytes) | **Category**: FW.crt | **Porting**: SK

Internal float-to-decimal output routine. Produces a decimal digit string from a floating-point value with specified precision. Used by `printf`-family for %f/%e/%g formatting.

---

### 83. _wcstombs @ 0x006056E0

**Size**: medium | **Category**: FW.crt | **Porting**: SK

CRT `wcstombs()`. Converts wide-character string to multibyte. Iterates calling `wctomb()` per character, respecting maxcount.

---

### 84. wcsncnt (A) @ 0x00605A40

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Counts wide characters in a string up to `maxlen` characters. Returns count (not byte count).

---

### 85. _getenv @ 0x00605AB0

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `getenv()`. Searches the environment block for a variable name. Calls `findenv()` to locate the entry, returns pointer to the value portion (after '=').

---

### 86. __setmode @ 0x00605BA0

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_setmode()`. Sets file translation mode (O_TEXT or O_BINARY) for a file descriptor. Returns old mode.

---

### 87. ___ld12mul @ 0x00605CE0

**Size**: large | **Category**: FW.crt | **Porting**: SK

12-byte long double multiplication. Multiplies two 12-byte long doubles, handling exponent addition, mantissa multiplication (schoolbook 32-bit limb multiply), normalization, and rounding.

---

### 88. ___multtenpow12 @ 0x00606110

**Size**: small | **Category**: FW.crt | **Porting**: SK

Multiplies a 12-byte long double by 10^power. Uses precomputed power-of-10 tables for efficiency. Sign parameter controls multiply vs divide.

---

### 89. __mbsnbicoll @ 0x006061F0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Case-insensitive multibyte string collation. Compares up to `maxcount` bytes using locale-aware comparison via `CompareStringA` with NORM_IGNORECASE.

---

### 90. ___wtomb_environ @ 0x00606260

**Size**: small | **Category**: FW.crt | **Porting**: SK

Converts wide-character environment strings to multibyte for the narrow environment block. Called during CRT initialization.

---

### 91. ___crtCompareStringW @ 0x00606330

**Size**: medium | **Category**: FW.crt | **Porting**: SK

CRT wrapper for `CompareStringW`. Falls back to multibyte conversion + `CompareStringA` if wide version unavailable. Caches API availability in DAT_0063b69c.

---

### 92. wcsncnt (B) @ 0x00606620

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Duplicate of `wcsncnt` (function 84). Counts wide characters up to maxlen. Exists at a different address for a different call site.

---

### 93. ___crtCompareStringA @ 0x00606690

**Size**: large | **Category**: FW.crt | **Porting**: SK

CRT wrapper for `CompareStringA`. Falls back to wide conversion + `CompareStringW` if needed. Handles locale-specific comparison with MBCS awareness.

---

### 94. _strncnt @ 0x00606AF0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Counts characters in a narrow string up to `maxcount`. Returns the count (not including null terminator).

---

### 95. ___crtsetenv @ 0x00606B60

**Size**: large | **Category**: FW.crt | **Porting**: SK

CRT internal `setenv` implementation. Parses "NAME=VALUE" string, finds or creates entry in environment block. Handles both primary (narrow) and secondary (wide) environment tables. Reallocates environment block if needed.

---

### 96. findenv @ 0x00606ED0

**Size**: small | **Category**: FW.crt | **Porting**: SK

Searches the environment block for a variable by name (case-insensitive, up to '='). Returns index into the environ array, or -1 if not found.

---

### 97. copy_environ @ 0x00606F70

**Size**: small | **Category**: FW.crt | **Porting**: SK

Copies the environment block. Allocates new pointer array + copies all strings. Used when environment needs to be modified (copy-on-write).

---

### 98. __mbschr @ 0x00607070

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_mbschr()`. Finds a character in a multibyte string. MBCS-aware: skips lead bytes of double-byte characters.

---

### 99. __filelength @ 0x006075E0

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_filelength()`. Returns file size in bytes. Uses `GetFileSize` on the OS handle obtained from the CRT fd.

---

### 100. FUN_006076a0 — `crt_get_struct_field_0x10` @ 0x006076A0

**Size**: stub | **Category**: FW.crt | **Porting**: SK

Trivial accessor: returns `*(param_1 + 0x10)`. Likely a CRT-internal struct field getter (sits between `_filelength` and `_strupr`).

---

### 101. __strupr @ 0x006076C0

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_strupr()`. Converts string to uppercase in-place. Uses locale-aware `LCMapStringA` with LCMAP_UPPERCASE when a non-C locale is active, otherwise ASCII toupper loop.

---

### 102. __strlwr @ 0x006077F0

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_strlwr()`. Converts string to lowercase in-place. Uses locale-aware `LCMapStringA` with LCMAP_LOWERCASE when a non-C locale is active, otherwise ASCII tolower loop.

---

### 103. FID_conflict:__mkdir @ 0x00607920

**Size**: small | **Category**: FW.crt | **Porting**: SK

CRT `_mkdir()`. Creates a directory. Wrapper around Win32 `CreateDirectoryA`. Returns 0 on success, -1 on failure with errno set.

---

## Verification

| Check | Value |
|-------|-------|
| Functions listed | 103 |
| Expected (FUNCTION_INDEX) | 103 |
| FW.crt count | 103 |
| SK count | 103 |
| Game logic functions | 0 |
| Functions needing pseudocode | 0 |
