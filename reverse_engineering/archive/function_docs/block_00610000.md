# Block 00610000 — Function Documentation
**Functions documented**: 2
**State-mutating functions**: 0

---

### FUN_0061a000 (0x0061A000, 326 bytes)
**Name**: unknown (possibly blit_rle_sprite_8bit or tile_copy_indexed)
**Category**: UI
**Calls**: (none — pure computation with segment-addressed memory)
**Called by**: unknown
**Globals read**: (none — uses FS_OFFSET-based segment addressing for data access)
**Globals written**: (none — writes go through indirect segment pointers, not to DAT_ globals)
**Summary**: 16-bit segmented-mode function that performs a 2D tile/sprite copy or blitting operation. It reads pixel values from a source buffer, applies a modular transformation, then conditionally writes them to a destination buffer. Uses nested loops iterating over rows and columns.
**State mutations**: NONE

**Detail**:
- This function uses 16-bit segmented memory addressing (in_SS, in_DS, unaff_FS_OFFSET), indicating it is legacy 16-bit code or a DOS-era sprite routine.
- Stack frame is set up manually: saves DS, SI, DI registers onto the stack.
- Reads two 32-bit far pointers from the stack (at offsets +0x1a and +0x1e), split into segment:offset pairs for source and destination.
- Reads loop bounds from stack parameters at offsets +0x28 (outer row count) and +0x24 (inner column count).
- Outer loop: decrements row counter (sVar19) from initial value down to 1.
- Inner loop: for each column, computes a source offset as `(row_offset + column_offset) * 2 + base`, reads a 16-bit value from the source segment.
- Computes `puVar12 = source[(row_start - current_col) * 2 + base + col_extra]`.
- Adds the source value and a second read value, then takes modulo by a divisor (from stack +0x26): `result = (val1 + val2) % divisor`.
- Looks up a byte from an indexed array via FS_OFFSET: `cVar13 = *(FS_OFFSET[base + result*4] + extra_offset + col_index)`.
- Compares against a filter value at stack +0x32; if filter is negative or the byte differs from the filter, writes the byte to the destination via a similar FS_OFFSET-indexed store.
- Returns void after both loops complete.

---

### FUN_0061a759 (0x0061A759, 423 bytes)
**Name**: blit_rle_sprite_16bit
**Category**: UI
**Calls**: (none — pure computation with segment-addressed memory)
**Called by**: unknown
**Globals read**: (none — all data access is through segment:offset pointers on the stack)
**Globals written**: (none — writes go through indirect pointers, not to DAT_ globals)
**Summary**: 16-bit segmented-mode function that decodes and blits an RLE (run-length encoded) sprite onto a destination buffer. It processes compressed sprite data row by row, handling transparency (skip) and pixel copy with a color key filter.
**State mutations**: NONE

**Detail**:
- Like FUN_0061a000, this is a 16-bit segmented function saving DS, SI, DI on the stack.
- Reads two far pointers from the stack (at +0x0a and +0x12/+0x16) for source/destination data.
- Reads a direction/sign value from stack +0x20. If negative, adjusts the destination base pointer calculation to flip the blit direction (for horizontal mirroring): `dest = base + abs(direction) * stride + column_start`.
- Computes initial destination pointer: `pcVar26 = (base_offset & 0xffff) + uVar19 * stride + column_start`.
- Reads the sprite row count from stack +0x26 into sVar25, which is the outer loop counter.
- Outer loop (per row):
  - Reads a 4-byte RLE header from the current data pointer: a 2-byte "skip" value (at offset -4) stored in local -0x14, and a 2-byte "count" value (at offset -2) stored in local -0x16 (sVar25).
  - If count is 0, skips this row entirely.
  - Advances a source index pointer by scanning forward through a column lookup table (segment uVar14 at offset sVar25), skipping columns where the lookup value is less than the skip value.
  - Decrements the column budget (sVar18) accordingly and advances pcVar26 (destination pointer) for each skipped column.
  - If columns remain (sVar18 > 0):
    - Reads a transparency/color-key byte from stack +0x1a into cVar12.
    - Inner pixel copy loop: for each remaining column, reads the pixel data offset from the lookup table, subtracts the skip value, and reads the actual pixel byte from `*(data_ptr + pixel_offset)`.
    - If the pixel byte differs from the color key (cVar12), writes it to `*pcVar26`.
    - Advances both source index (sVar25 += 2) and destination pointer (pcVar26 += 1).
    - Loop terminates when count reaches 0 or the data offset exceeds the row length.
  - After the row, restores pcVar26 to the saved row start, advances it by the direction stride (stack +0x20).
  - Advances the RLE data pointer (local -0x10) by 2.
  - Decrements the row counter; returns when all rows are processed.
